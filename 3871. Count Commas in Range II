class Solution {
    public long countCommas(long n) {
        long t=0;
        long a=1000;
        while(n>=a){
            t+=(n-a+1);
            if(a>Long.MAX_VALUE/1000)break;
            a*=1000;
        }return t;
    }
}
