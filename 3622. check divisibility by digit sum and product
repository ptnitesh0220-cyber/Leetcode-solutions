class Solution {
    public boolean checkDivisibility(int n) {
        int a=n;
        int s=0,p=1;
        while(a!=0){
            s+=a%10;
            p=p*(a%10);
            a=a/10;
        }
        int sum=s+p;
        if(n%sum==0)return true;
        return false;
    }
}
