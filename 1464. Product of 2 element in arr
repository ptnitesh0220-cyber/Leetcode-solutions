int maxProduct(int* nums, int numsSize) {
    int n=numsSize;
    int m1=0,m2=0;
    for(int i=0;i<n;i++){
        int num=nums[i];
        if(m1<num){
            m2=m1;
            m1=num;
        }
        else if(m2<num)
        m2=num;
    }
    return (m1-1)*(m2-1);
}
