class Solution {
    public int rob(int[] nums) {
        int a=0,b=0;
        for(int x:nums){
            int temp=a;
            a=Math.max(a,b+x);
            b=temp;
        }
        return a;
    }
}
